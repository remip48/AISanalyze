% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AISinfos.R
\name{AISinfos}
\alias{AISinfos}
\title{Estimate vessel characteristics}
\usage{
AISinfos(
  ais_data,
  threshold_length = 475,
  threshold_draught = 30,
  threshold_width = 75,
  weight_complete_data = 10
)
}
\arguments{
\item{ais_data}{AIS data frame containing the columns \code{mmsi}, \code{shiptype},
\code{length}, \code{width}, \code{draught}, \code{imo}, and \code{name}. Another vessel identifier
may be used if the column is named \code{mmsi}.}

\item{threshold_length}{Maximum valid vessel length (m). Larger values are
set to \code{NA}.}

\item{threshold_draught}{Maximum valid draught (m). Larger values are set
to \code{NA}.}

\item{threshold_width}{Maximum valid vessel width (m). Larger values are
set to \code{NA}.}

\item{weight_complete_data}{Weight assigned to records containing both
vessel length and ship type.}
}
\value{
A list containing:
\itemize{
\item \code{estimated_values}: Estimated vessel characteristics for each \code{mmsi}.
\item \code{summary}: Summary statistics for each \code{mmsi}, including:
\itemize{
\item Number of AIS positions.
\item Number of non-missing values for \code{length}, \code{shiptype}, \code{width},
\code{draught}, \code{imo}, and \code{name}.
\item All valid values observed for each characteristic.
\item The most likely value for each characteristic.
}
}
}
\description{
Estimates the most likely vessel characteristics for each \code{mmsi} from AIS
messages, including ship type, length, width, draught, IMO number, and
vessel name. Estimates are based on the most frequent values, giving greater
weight to records with more complete information.
}
\examples{
\dontrun{
library(AISanalyze)
data("ais")

out <- AISinfos(ais_data = ais)}
}
