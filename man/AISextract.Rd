% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AISextract.R
\name{AISextract}
\alias{AISextract}
\title{Extract AIS positions around target locations and times}
\usage{
AISextract(
  ais_data,
  data,
  crs_meters = 3035,
  return_all_vessel_locations = TRUE,
  search_into_radius_m = 50000,
  search_shape = "circle",
  interval_time_before = 5 * 60,
  interval_time_after = 5 * 60,
  nb_cores = 1,
  outfile = tempfile()
)
}
\arguments{
\item{ais_data}{AIS data frame containing \code{timestamp}, \code{lon}, \code{lat}, and
\code{mmsi}. \code{timestamp}, \code{lon}, and \code{lat} must be numeric. Another vessel
identifier may be used if the column is named \code{mmsi}.}

\item{data}{Data frame containing \code{timestamp}, \code{lon}, and \code{lat}.
\code{timestamp} must be Unix time (seconds since 1970-01-01), while \code{lon}
and \code{lat} must be numeric.}

\item{crs_meters}{CRS (metres) used to calculate distances. Defaults to
EPSG:3035.}

\item{return_all_vessel_locations}{Logical. If \code{TRUE}, returns all vessel
positions within the specified time window. Otherwise, returns only the
closest position in time.}

\item{search_into_radius_m}{Search radius (m).}

\item{search_shape}{\code{"circle"} (default; selects vessels within
\code{search_into_radius_m} of the target location) or \code{"square"} (selects
vessels within \code{search_into_radius_m} in both the X and Y directions,
useful for grid-based analyses).}

\item{interval_time_before}{Time window (s) before each \code{data$timestamp}.}

\item{interval_time_after}{Time window (s) after each \code{data$timestamp}.}

\item{nb_cores}{Number of CPU cores used.}

\item{outfile}{File used to save logs.}
}
\value{
\code{data} joined with matching AIS positions. Rows are duplicated when
several vessel positions match a target location and time. If no vessel is
found, AIS columns (including \code{mmsi}) are filled with \code{NA}. The output also
includes \code{distance_vessel_to_location_m}, the distance (m) between the
target location and vessel positions.
}
\description{
Returns either (depending on \code{return_all_vessel_locations}):
\itemize{
\item each vessel position at the target timestamps.
\item or all vessel positions within a specified time window.
}
}
\examples{
library(AISanalyze)
data("ais")
data("point_to_extract")

# Define the Unix time (seconds since 1970-01-01)
point_to_extract$timestamp <- as.numeric(lubridate::ymd_hm(point_to_extract$datetime))
ais$timestamp <- as.numeric(lubridate::ymd_hms(ais$datetime))

# calculate the travelled distance, time, speed, and interpolate AIS data:
ais <- ais |>
  AIStravel() |>
  AISinterpolate(type_interpolation = "exact_timestamp",
                 exact_timestamp = list(
             timestamp_to_interpolate = point_to_extract$timestamp,
             locations_of_interest = data.frame(lon = point_to_extract$lon,
                                                lat = point_to_extract$lat),
             radius = 200000),
                 crs_meters = 3035)

# Extract all vessel positions within the target time interval and radius:
out <- AISextract(ais_data = ais,
           data = point_to_extract,
           crs_meters = 3035,
           return_all_vessel_locations = TRUE,
           search_into_radius_m = 50000,
           interval_time_before = 5 * 60,
           interval_time_after = 5 * 60)

# Extract each vessel position at the target timestamps within the radius:
out <- AISextract(ais_data = ais,
           data = point_to_extract,
           crs_meters = 3035,
           return_all_vessel_locations = FALSE,
           search_into_radius_m = 50000,
           interval_time_before = 5 * 60,
           interval_time_after = 5 * 60)
}
